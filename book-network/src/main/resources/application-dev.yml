spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_social_network
    username: username
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSLDialect
  mail:
    host: localhost
    port: 1080
    username: ahmed
    password: ahmed
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectionTimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  security:
    jwt:
      secretKey: 64028a6e00a311b5776e3770d5972b44b6a70d96
      expiration: 86400000